<h1>New task</h1>

<%# Display form for new task %>
<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :details %>
  <%= f.text_field :details %>

  <%= f.submit %>
<% end %>

<%# Return to index %>
<%= link_to('Back to list', tasks_path) %>
